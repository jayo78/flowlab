import { extendTheme } from '@chakra-ui/react';

const customTheme = extendTheme({
    colors: {
        primary: '#6c75f1',
        secondary: '#f16c75',
        accent: '#75f16c'
    }
});

export default customTheme;
